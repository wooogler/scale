node_modules
dist
*.tsbuildinfo
.DS_Store
coverage/
.vite/

# Coverage-memory reverse index — derived from every doc's `sources`, rebuilt
# on demand by `scale map index`. The docs and map.json are committed; this is
# not (PLAN §4.2).
.scale/index.json

# graphify build intermediate — the distilled artifacts under .scale/ are what we
# commit; the raw graph is regenerable and large (PLAN-GRAPHIFY §3).
graphify-out/
